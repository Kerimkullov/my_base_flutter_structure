import 'package:flutter/material.dart';

abstract class MyBeelineGradients {
  // Example of creating Gradients
  static const gradientBlue = LinearGradient(
    colors: [
      Color(0XFF006DEB),
      Color(0XFF338FEF),
    ],
  );
}
